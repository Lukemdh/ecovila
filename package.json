{
  "name": "ecovila",
  "version": "0.0.0",
  "private": true,
  "description": "EcoVila static frontend and Supabase Edge Function test scripts.",
  "scripts": {
    "test": "npm run test:node && npm run test:deno",
    "test:node": "node --test 'docs/tests/**/*.test.mjs'",
    "test:deno": "cd docs/supabase/functions && deno task test"
  }
}
